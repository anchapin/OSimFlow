# Contributing to OSimFlow

> **Status:** Placeholder. Full contributor onboarding is a Phase 3 deliverable per PRD §5.2 / §6.

When this is implemented, this file will cover:

- How to set up a development environment (Nextflow, Docker, OpenStudio CLI locally).
- Coding standards (mirroring the rules in [`../AGENTS.md`](../AGENTS.md) §6).
- How to propose a new Nextflow module or extend the BYOS contract.
- How to run the end-to-end test suite (when it exists in `tests/`).
- PR review process and CI expectations.
- How to add a new OpenStudio CLI version to the `ghcr.io/anchapin/openstudio_cli_image` build matrix.
- Community channels (GitHub Discussions, Matrix/Slack, monthly call) — *to be decided*.

For now, please open an issue with the `proposal` or `question` label to start a conversation.
